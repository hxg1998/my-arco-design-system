# My Arco Design System

这是本项目唯一的 UI/设计规范来源。

规则：
1. 默认使用 Arco Design React 组件作为基础组件。
2. 优先复用本仓库中的封装组件，不要新造基础组件。
3. 严格遵循 docs/ 中的设计规范与页面模式。
4. 如现有组件不能满足需求，先说明缺少什么，再扩展。
5. 开发完成后，必须按 docs/acceptance-checklist.md 自检。
